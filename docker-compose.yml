services:
  influxdb:
    image: influxdb:2.7
    ports:
      - "8086:8086"
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: yp
      DOCKER_INFLUXDB_INIT_PASSWORD: yp-ground-station
      DOCKER_INFLUXDB_INIT_ORG: yp
      DOCKER_INFLUXDB_INIT_BUCKET: telemetry
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: yp-dev-token
    volumes:
      - influx-data:/var/lib/influxdb2

  yp-server:
    build:
      context: ./services/server
    ports:
      - "8000:8000"
    environment:
      INFLUX_URL: http://influxdb:8086
      INFLUX_ORG: yp
      INFLUX_BUCKET: telemetry
      INFLUX_TOKEN: yp-dev-token
      TILE_DIR: /data/tiles
      TILE_CACHE_DIR: /data/tile-cache
      OSM_TILE_URL: https://tile.openstreetmap.org/{z}/{x}/{y}.png
      OSM_USER_AGENT: YPGroundStation/0.1
      OSM_REFERER: http://localhost:8080/
      EARTH_TILE_URL: https://services.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}
      EARTH_USER_AGENT: YPGroundStation/0.1
      EARTH_REFERER: http://localhost:8080/
      VEHICLE_TTL_SECONDS: "30"
    volumes:
      - ./data/tiles:/data/tiles:ro
      - ./data/tile-cache:/data/tile-cache
    depends_on:
      - influxdb

  web:
    build:
      context: ./web
    ports:
      - "8080:80"
    depends_on:
      - yp-server

  yp-gps:
    build:
      context: ./services/yp_gps
    environment:
      SERVER_WS_URL: ws://yp-server:8000/ws/vehicle
      GPS_MODE: sim
      VEHICLE_ID: yp
      HOME_LAT: "38.984764"
      HOME_LON: "-76.478643"
      HOME_ALT: "2.0"
      HEADING_DEG: "330"
      SPEED_KNOTS: "3"
      SEND_HZ: "5"
      SERIAL_PORT: /dev/ttyUSB0
      BAUD_RATE: "9600"
    depends_on:
      - yp-server
    # For a real serial GPS, switch GPS_MODE to serial and uncomment:
    # devices:
    #   - /dev/ttyUSB0:/dev/ttyUSB0

  sim-uav:
    build:
      context: ./services/sim_vehicle
    environment:
      SERVER_WS_URL: ws://yp-server:8000/ws/vehicle
      VEHICLE_TYPE: uav
      VEHICLE_ID: sim-uav
      HOME_LAT: "38.98490"
      HOME_LON: "-76.47880"
      HOME_ALT: "45.0"
      SPAWN_JITTER_DEG: "0.0003"
      TARGET_JITTER_DEG: "0.0010"
      SEND_HZ: "5"
    depends_on:
      - yp-server

  sim-usv:
    build:
      context: ./services/sim_vehicle
    environment:
      SERVER_WS_URL: ws://yp-server:8000/ws/vehicle
      VEHICLE_TYPE: usv
      VEHICLE_ID: sim-usv
      HOME_LAT: "38.98465"
      HOME_LON: "-76.47845"
      HOME_ALT: "0.0"
      SPAWN_JITTER_DEG: "0.0003"
      TARGET_JITTER_DEG: "0.0010"
      SEND_HZ: "5"
    depends_on:
      - yp-server

  sim-uuv:
    build:
      context: ./services/sim_vehicle
    environment:
      SERVER_WS_URL: ws://yp-server:8000/ws/vehicle
      VEHICLE_TYPE: uuv
      VEHICLE_ID: sim-uuv
      HOME_LAT: "38.98455"
      HOME_LON: "-76.47875"
      HOME_ALT: "-6.0"
      SPAWN_JITTER_DEG: "0.0003"
      TARGET_JITTER_DEG: "0.0010"
      SEND_HZ: "5"
    depends_on:
      - yp-server

volumes:
  influx-data:
